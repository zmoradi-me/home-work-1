C2 = ifelse(C == 2, 1, 0)
C3 = ifelse(C == 3, 1, 0)
C4 = ifelse(C == 4, 1, 0)

S2 = ifelse(S == 2, 1, 0)
S3 = ifelse(S == 3, 1, 0)


#

nloglik_loglinear <- function(theta) {
  
  
  beta0 <- theta[1]
  beta1 <- theta[2]
  beta2 <- theta[3]
  beta3 <- theta[4]
  beta4 <- theta[5]
  beta5 <- theta[6]
  beta6 <- theta[7]
  beta7 <- theta[8]
  
  n <- length(Sa)
  prob_vector <- numeric(n)
  

  for (i in 1:n) {
    
    
    eta_i <- beta0 + beta1*C2[i] + beta2*C3[i] + beta3*C4[i] + 
             beta4*S2[i] + beta5*S3[i] + beta6*W[i] + beta7*Wt[i]
    
  
    lambda_i <- exp(eta_i)
    
    
    prob_vector[i] <- dpois(Sa[i], lambda_i)
  }
  
 
  likelihood <- prod(prob_vector)
  
  return(-log(likelihood))
}

# initial_theta <- rep(0, 8)
# result <- optim(par = initial_theta, fn = nloglik_loglinear, method = "BFGS")
# print(result$par)
